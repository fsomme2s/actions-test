name: Greet Action
description: greet people
inputs:
  my-secret:
    description: 'tell me a secret'

runs: 
  using: 'composite'
  steps:
    - shell: bash
      run: |
        echo "hello world!"
      